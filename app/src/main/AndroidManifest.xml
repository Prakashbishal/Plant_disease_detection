<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.testingg">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/pddlogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/pddlogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Testingg"
        tools:targetApi="31">

        <activity
            android:name=".ui.home.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Testingg.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Testingg.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



        <!-- LoadingActivity -->
        <activity android:name=".ui.home.LoadingActivity" />

        <!-- PreventionsActivity -->
        <activity
            android:name=".ui.home.PreventionsActivity"
            android:label="Preventions"
             />

    </application>

</manifest>
